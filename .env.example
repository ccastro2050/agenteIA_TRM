# ============================================================
# .env.example — Agente IA Colombia · agenteIA_TRM
# ============================================================
# Copia este archivo a .env y reemplaza los valores de ejemplo
# con tus claves reales.
#
# IMPORTANTE: el archivo .env NUNCA se sube a GitHub.
# Este .env.example SÍ se sube (claves de ejemplo, no reales).
#
# Mínimo obligatorio para arrancar el proyecto:
#   1. OPENAI_API_KEY  — para embeddings (text-embedding-3-small)
#   2. PG_PASSWORD     — contraseña de PostgreSQL
#   3. La API key del proveedor LLM que elijas
# ============================================================


# ── Servidor FastAPI ─────────────────────────────────────────
HOST=0.0.0.0
PORT=8001


# ── Proveedor LLM ───────────────────────────────────────────
# Opciones: anthropic | openai | deepseek | ollama | qwen | zhipu | moonshot
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini

# Claves API — completa solo el proveedor que usarás
ANTHROPIC_API_KEY=sk-ant-api03-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX-XXXXXXXX
OPENAI_API_KEY=sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
DEEPSEEK_API_KEY=sk-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
QWEN_API_KEY=sk-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
ZHIPU_API_KEY=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX.XXXXXXXXXXXXXXXX
MOONSHOT_API_KEY=sk-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# URL base personalizada (solo si usas proxy o endpoint propio)
# LLM_BASE_URL=http://mi-servidor:8080/v1


# ── Embeddings ───────────────────────────────────────────────
# El agente RAG requiere OpenAI para text-embedding-3-small.
# Incluso si el LLM es DeepSeek o Anthropic, los embeddings usan OpenAI.
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small


# ── RAG / Chunking ───────────────────────────────────────────
RETRIEVAL_K=4
CHUNK_SIZE=800
CHUNK_OVERLAP=100


# ── pgvector (PostgreSQL) ────────────────────────────────────
# Instalación rápida con Docker:
#   docker run -d --name pgvector \
#     -e POSTGRES_PASSWORD=mipassword \
#     -e POSTGRES_DB=bdvector \
#     -p 5432:5432 pgvector/pgvector:pg16
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=bdvector
PG_USER=postgres
PG_PASSWORD=mipassword
PG_COLLECTION=dane_reportes


# ── LangSmith (opcional) ─────────────────────────────────────
# Si LANGSMITH_API_KEY está vacío, LangSmith queda deshabilitado.
# Crea una cuenta en https://smith.langchain.com
LANGSMITH_API_KEY=lsv2_pt_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX_XXXXXXXXXXXXXXXX
LANGSMITH_PROJECT=agenteIA-TRM
